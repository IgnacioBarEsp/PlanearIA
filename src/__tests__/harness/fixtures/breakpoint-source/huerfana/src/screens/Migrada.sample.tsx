// Fixture: el arbol no contiene src/hooks/useBreakpoint.ts, asi que la entrada
// autorizada quedo huerfana. La pantalla en si esta sana.
export function useAnchoMigrado(): number {
  return 1024;
}
